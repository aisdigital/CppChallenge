#pragma once

#include <string>

using std::string;

/**
 * @brief Returns a lowercase representation of a string.
 * 
 * @param str String to get lowercase representation.
 * 
 * @return Lowercase version of string.
 */
string stringToLower(string str);
