{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CppChallenge",
            "type": "shell",
            "command": "cmake . -B ./build && make -C ./build/ CppChallenge && ./build/CppChallenge"
        }
    ]
}